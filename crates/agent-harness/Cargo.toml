[package]
name = "libre-ai-agent-harness"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
serde.workspace = true
serde_jcs.workspace = true
sha2.workspace = true

# Only the tests read the locked digest vector; the library itself never parses
# JSON, it only canonicalizes what a caller already holds.
[dev-dependencies]
serde_json.workspace = true
