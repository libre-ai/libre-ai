# Banc d'essai des mécanismes de dépendance (design §5.2.6) — exécuté 2026-07-28

Fixtures sous le scratchpad de session (`bench/`), hors dépôt. À verser en évidence avec la phase 3.2.

| Cas                                                                                      | Résultat                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Preuve                                                                  |
| ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 1. Git-dep Bun consommant un package à exports `bun` (sans `dist/`, sans lifecycle)      | **Prouvé partiellement** : la résolution d'exports `bun` → source TS fonctionne pour un package non-workspace installé sans build (`bun run` exécute la source). **Limite de transport** : bun ne supporte pas `git+file://` (échec `git failed with exit code 128` même par ref, clone git direct OK) — le banc local ne peut pas exercer le transport git ; le pin `github:libre-ai/<repo>#<sha>` reste à confirmer au premier satellite réel (GitHub sert les SHAs via allowReachableSHA1InWant) | fixture `bench/satellite-ui` + `bench/consumer`, sortie `bench:git-dep` |
| 2. `tsc --noEmit` à travers la dépendance (pas de `.d.ts`)                               | **Prouvé** : le consommateur type-vérifie la source TS de la dep (strict, moduleResolution bundler) sans erreur ; le coût à l'échelle des vrais satellites se mesurera au premier                                                                                                                                                                                                                                                                                                                   | exit 0 sur la fixture                                                   |
| 3. Copie vendorée byte-exacte sous gate de dérive                                        | **Prouvé par l'existant** : `packages/contracts/sync-schemas.ts --check` tourne en CI (« Vendored schemas are byte-exact against contracts/schemas », étape generated-contracts de la chaîne de référence)                                                                                                                                                                                                                                                                                          | chaîne de référence 10/10 du 2026-07-28                                 |
| 4. Patch `aes` re-déclaré par le workspace consommateur d'une git-dep                    | **Prouvé** : `[patch.crates-io] aes = { path = third_party/rustcrypto-aes-0.8.4 }` déclaré par le consommateur s'applique au graphe entier, y compris à la dépendance `aes` d'un crate obtenu par git-dep — `cargo tree -i aes` résout vers le chemin du patch                                                                                                                                                                                                                                      | fixture `bench/sat-crate` + `bench/consumer-rs`                         |
| 5. `[sources.allow-org] github = ["libre-ai"]` sur cargo-deny 0.19.5 (version CI exacte) | **Prouvé avec contre-preuve** : `cargo deny check sources` → `sources ok` (exit 0) avec la clé, `error[source-not-allowed]` (exit 8) sans — sur une **vraie git-dep GitHub** `libre-ai-artifact @ 5c1bf05…`                                                                                                                                                                                                                                                                                         | fixture `bench/consumer-deny`                                           |

Bonus prouvé (cas 5) : **cargo résout un crate membre d'un workspace via git-dep** (le repo hub entier comme source, crate ciblé par nom) — utile pendant la transition, avant l'éclatement des workspaces Rust.

Findings de banc pour le plan de migration (3.3/3.4) :

1. Le SHA-pin Bun sur transport GitHub est le seul mécanisme non exercé localement — à confirmer comme premier acte du premier satellite (avant tout retrait de chemin au hub).
2. Bun n'exécute aucun lifecycle de la git-dep (confirmé par construction — aucun `dist/` produit) : la condition d'exports `bun` est la seule servie, conformément au design §5.2.2.
3. `uploadpack.allowAnySHA1InWant` n'aide pas les transports `file://` — les fixtures de banc futures devront utiliser des refs, ou GitHub.
