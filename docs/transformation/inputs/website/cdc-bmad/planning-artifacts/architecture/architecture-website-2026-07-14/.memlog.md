---
scope: Refonte libre-ai.fr — site statique vitrine du collectif
purpose: Spine d'invariants pour construire le site depuis le PRD + spines UX (CDC)
altitude: initiative→features
updated: 2026-07-14T11:34
---

- (constraint) invariants hérités (PRD §11, non re-dérivables) : marque Libre IA + libre-ai.fr, souveraineté NFR-1 (zéro dépendance extraterritoriale), zéro-tracking NFR-2, exigence de vérité FR-1/NFR-11, budget sobriété NFR-6, hébergement Clever Cloud statique acquis (clevercloud.json)
- (decision) mode : Coaching (forks présentés avec alternatives pesées, arbitrage stakeholder) ; livrable = spine seule, deck HTML re-proposé au finalize
- (decision) AD candidat — framework : Dioxus 0.7 SSG conservé (arbitré) ; domaine éditorial Rust typé = enforcement du gabarit FR-1 par le type system ; rendu HTML statique sans hydratation, JS progressif seulement
- (decision) AD candidat — contenu : pièces du corpus en markdown strict + front matter, parsées dans le domaine typé (pulldown-cmark), build échoue si gabarit non satisfait ; clause de réversibilité : sortie vers SSG markdown documentée (contenu + tokens survivent), déclencheur = JS obligatoire ou budget sobriété cassé
- (version) dioxus 0.7.x cycle courant vérifié web (SSG natif) ; repo déjà en 0.7.9
- (decision) AD — conversion tout-maison (arbitré, option I) : UN seul service dynamique « conversion » en Rust/axum sur Clever Cloud ; porte le formulaire (honeypot+rate-limit → SMTP) ET la prise de RDV maison minimale
- (decision) AD — bar UX minimal du booking maison : créneaux réservables sans compte, confirmation instantanée, ICS joint à l'email, annulation par lien signé ; sans JS le flux dégrade en POST classique rendu par le service
- (decision) AD — frontières : le service conversion possède son stockage propre (addon dédié), ne partage aucune donnée avec quoi que ce soit ; données minimales nom/org/email/message, rétention courte, zéro PII en logs
- (decision) AD — risque accepté et clause de sortie : UX booking maison vs métrique M1 — si les RDV ne viennent pas et que la réservation est en cause, bascule vers outil dédié auto-hébergé (invariant licence : MIT/Apache/MPL/GPL, AGPL sur dérogation explicite, données EU, zéro tracking)
- (decision) AD — déployables : monorepo website, deux déployables Clever Cloud (site statique + service conversion), domaines défensifs en 301 vers libre-ai.fr ; SMTP différé derrière une interface d'envoi abstraite
- (decision) AD — gates CI (validés) : éditorial FR-1 bloquant au build ; sobriété NFR-6 mesurée en CI seuil bloquant et chiffres publiés issus de la mesure ; accessibilité automatisable bloquante + clavier en e2e ; scan zéro-tiers bloquant ; scan confidentialité (noms non autorisés + chemins machine-local) bloquant ; approbation humaine avant tout déploiement production
- (event) coaching clos — 5 forks arbitrés, passage au finalize (distill → reconcile → reviewer gate → Gate 4)
- (event) reviewer gate résolu : lint propre ; rubrique (nature+ops+gouvernance gates corrigés) ; web-verify (SMTP EU non confirmés retirés, versions dioxus/axum/pulldown vérifiées) ; adversarial (5 resserrements appliqués, dont slots-au-build REJETÉ au profit de état-des-créneaux-exclusif-au-service) ; réconciliation (citation/OG/footer-proof/thème sombre/registre couverts)
- (event) spine finalized — GATE 4 : GO du stakeholder
