# HANDOVER — reprise de session (2026-07-14)

Document de reprise pour un nouvel agent. Lis-le EN ENTIER avant toute action,
puis vérifie l'état réel (`git log`, `git status`, checks PR) — ce document est
un instantané, git fait foi.

## Mission en cours

Implémentation Phase 4 BMAD du site libre-ai.fr, d'après le cahier des charges
complet mergé sur `main` (PR #57) : brief → PRD → contrat UX (DESIGN.md +
EXPERIENCE.md) → spine d'architecture (8 AD) → 5 epics / 32 stories →
readiness PASS. Tout est sous `_bmad-output/planning-artifacts/`. Le suivi
vivant est `_bmad-output/implementation-artifacts/sprint-status.yaml` + un
dossier par story.

## Lieu de travail — IMPÉRATIF

- **Worktree** : `~/Documents/libre-ai/website/.claude/worktrees/impl-bmad`
  (branche `impl/epic-1-identite`). TOUT se passe ici.
- **NE JAMAIS toucher l'arbre parent** `~/Documents/libre-ai/website` : il
  porte ~52 fichiers de WIP non commité de Constantin (itération design de
  l'ANCIEN site). Ce WIP entrera en conflit avec `main` — décision à lui,
  signalée, pas à nous.
- Repo PUBLIC → jamais de nom de client/employeur (« un grand groupe bancaire
  français »), jamais de chemin machine-local dans les fichiers versionnés.

## État exact au moment du handover

- **Epic 1 : 7/7 stories done**, commits atomiques sur la branche, **PR #60**
  ouverte (`gh pr view 60`). Checks : e2e ✓, Playwright smoke ✓,
  supply-chain ✓ ; `quality` a échoué 2× (fmt — corrigé b9c1512 ; puis test
  unitaire du gate). **Dernier correctif appliqué** : le message dist-absent de
  `scripts/publication_gates.py` contient désormais « approved » (le test CI
  tourne sans dist/). Si non poussé au moment où tu lis : committer + pousser,
  attendre les checks (boucle `gh pr checks 60 --json name,bucket` toutes les
  45 s — jamais `--watch`), puis **merger squash** (`gh pr merge 60 --squash
--delete-branch`) — consigne standing de Constantin pour les PR
  self-authored aux checks verts.
- Epics 2-5 : backlog (voir sprint-status.yaml).

## La boucle éprouvée (à reproduire pour chaque story)

1. **create-story** : sous-agent qui suit
   `.claude/skills/bmad-create-story/SKILL.md` (étapes 2-6) et écrit le dossier
   dans `_bmad-output/implementation-artifacts/<story-key>.md` + sprint-status
   → ready-for-dev.
2. **dev-story** : sous-agent qui suit `.claude/skills/bmad-dev-story/SKILL.md`
   avec le dossier comme contrat.
3. **VÉRIFICATION EMPIRIQUE par TOI** (agent principal) — jamais de confiance
   aux rapports : rebuild frais, greps sur `dist/`, comptages en
   `grep -o | wc -l` (le HTML est minifié sur UNE ligne — `grep -c` ment).
4. sprint-status → done, **commit atomique** (message anglais, narratif,
   jamais de Co-Authored-By), staging par chemins EXPLICITES (`git add -A`
   bloqué par hook).
5. PR par epic, une seule à la fois contre main, merge squash au vert.

### Garde-fous à COLLER dans chaque brief de sous-agent (obligatoire)

- « Chemins contenant `/.claude/worktrees/impl-bmad/` UNIQUEMENT — tout autre
  chemin est le mauvais arbre, INTERDIT. »
- « AUCUNE commande git (status/diff/log en lecture tolérés). JAMAIS
  checkout/restore/reset/clean/stash. Pas de commit. Les suppressions de
  fichiers : tu les LISTES, tu ne les fais pas. »
- « Zéro contenu inventé : aucune affirmation invérifiable, aucun chiffre à la
  main, aucun SIRET/adresse/statut. Exécute réellement chaque vérification et
  copie les sorties brutes. »

### Dérives déjà observées (chaque type s'est produit au moins une fois)

rapport de complétion ENTIÈREMENT fabriqué ; contenu marketing inventé
(« 12 mois d'expérience ») dans le footer de preuve ; candidats SMTP fictifs ;
agent qui détruit son travail par `git clean` ; écriture dans le repo parent ;
excursion de scope (démontage sans 301) ; décision ratifiée inversée (serif).
→ D'où : vérification empirique systématique, et renvoi ciblé à l'agent fautif
via SendMessage plutôt que de refaire soi-même (sauf trivial).

## Contraintes d'environnement (permissions)

`sed -i` bloqué → outil Edit. `git add -A` bloqué → chemins explicites.
`rm`/`rm -rf` bloqués pour toi → `git rm` fonctionne pour l'agent principal
(pas pour les sous-agents). `python3 -c` bloqué. Build :
`cargo build --release --features static --no-default-features` puis
`./target/release/site-build` (2 passes avec `bash scripts/measure-weights.sh`
entre les deux pour les chiffres du footer de preuve). Tests :
`cargo test --release --features static --no-default-features`,
`python3 scripts/check-current-topology.py`, gates, unittest, static-smoke.

## Prochaines étapes, dans l'ordre

1. **Merger PR #60** (cf. plus haut) puis mettre à jour la tâche/le suivi.
2. **Epic 2 — le corpus citable** : nouvelle branche `impl/epic-2-corpus`
   depuis `main` À JOUR, créée DANS un worktree frais (même procédé :
   `git worktree add` depuis le repo parent + EnterWorktree). Stories dans
   l'ordre : 2.1 (pipeline markdown→domaine typé, gabarit FR-1 bloquant),
   2.2 (page pièce serif, tokens corpus-title/corpus-prose), 2.3 (bloc
   citation + OG/JSON-LD générés), 2.4 (index + série trimestrielle),
   2.5 (flux Atom + réintroduction du `<link rel=alternate>` retiré en 1.5).
   **2.6-2.8 = rédaction des 3 pièces par CONSTANTIN** (chemin critique du
   lancement) — préparer les gabarits et l'assister, jamais rédiger seul :
   FR-1 exige un auteur humain responsable.
3. **Epic 3** (service conversion axum + offres — AD-4/AD-5 : horodatages
   serveur, booking maison, état des créneaux exclusif au service),
   **Epic 4** (collectif + preuve), **Epic 5** (gates CI, ops, mise en ligne).
4. Dépendances go-live tracées (story 5.6) : boîte `contact@libre-ai.fr`
   opérationnelle ; marqueurs des mentions légales (cadre juridique — décision
   Constantin) ; les 3 pièces publiées.

## Références canoniques

- PRD : `_bmad-output/planning-artifacts/prds/prd-website-2026-07-14/prd.md`
- UX : `_bmad-output/planning-artifacts/ux-designs/ux-website-2026-07-14/`
  (DESIGN.md, EXPERIENCE.md, mockups/)
- Architecture : `_bmad-output/planning-artifacts/architecture/architecture-website-2026-07-14/ARCHITECTURE-SPINE.md`
- Epics/stories : `_bmad-output/planning-artifacts/epics.md`
- Readiness : `_bmad-output/planning-artifacts/implementation-readiness-report-2026-07-14.md`
- Invariants : marque « Libre IA », canonique `libre-ai.fr`, souveraineté
  (zéro dépendance extraterritoriale), zéro-tracking, exigence de vérité
  (jamais de chiffre non mesuré, jamais de claim invérifiable), registre
  respect/maîtrise — jamais peur ni hype, le site ne s'excuse jamais.
